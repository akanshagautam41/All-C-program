#include<stdio.h>

main()
{
	int n,sum;
	printf("\n Enter any number:");
	scanf("%d",&n);
	 if(sum=n/2)
	 {
	 	printf("\n its a natural number:");
	 }
	 else
	 {
	 	printf("\n its not natural number");
	 }
}
